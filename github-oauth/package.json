{
  "name": "bookmarkdown-github-oauth",
  "version": "1.0.0",
  "description": "GitHub OAuth service for BookMarkDown using Cloudflare Worker",
  "main": "src/index.ts",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --outfile=dist/worker.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "keywords": [
    "oauth",
    "github",
    "cloudflare-worker",
    "bookmarkdown"
  ],
  "author": "BookMarkDown Contributors",
  "license": "Apache-2.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "esbuild": "^0.21.5",
    "wrangler": "^3.84.1"
  }
}